# Debrief: TODO 1-5

## Task summary

Implemented `docs/TODO.md` items 1-5 by requiring explicit `working_dir` on dispatch tools, broadening Claude's default tool allowlist, adding low-context alternatives to the `read`/`transfer`/`poll` tool descriptions, setting a real local `MAESTRO_DEFAULT_REPO`, and documenting Claude.ai MCP tool filtering in the README.

## Files changed

- `maestro/tools/fleet.py`: Removed the `working_dir` default from `codex`, `gemini`, and `claude`, expanded Claude's default `allowed_tools`, and appended the requested docstring guidance for `read`, `transfer`, and `poll`.
- `.env`: Added `MAESTRO_DEFAULT_REPO="/home/user/Development"` as the local fallback path for agent tools on this machine.
- `.env.example`: Documented `MAESTRO_DEFAULT_REPO` with a comment and a concrete example value.
- `README.md`: Added a `Gotchas` section near the bottom describing Claude.ai MCP connector filtering for tools that look like credential generation.

## Surprises or decisions made

- `.env` is git-ignored here, so I updated it locally but did not force-add secrets into git; the item 4 commit contains only the tracked `.env.example` change.
- `gemini` needed its parameters reordered so `working_dir` could be a real required argument in Python while leaving the underlying runtime behavior unchanged.
